#include <stdio.h>

struct Point {
  int x;
  int y;
};

bool clamp(int a, int b) 
{
  int c = 25;
  int ret = (a > b)? a: b;
  return ret > c;
}

int main(int argc, char * argv[]) 
{
  float a = -2.0;
  bool variable;
  int i = 0;
  struct Point *point;
  int someArray[25];
    
  point->x = 7;
  for (i = 0; ; i++)
  {
	  someArray[i] = i * 2;
	  if (i == 25)
	  {
		  break;
	  }
  }

  for (i = 0; i < a; ++i) {
    if (a + i > 0) {
      c = false;
    } else {
      int z = 15;
    }
  }
  someArray[0] = 13;
  scanf("%d", &a);
  printf("%d", a);
  return 0;
}